<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="mapjson.com.mapapplication.MainActivity">
    <CheckBox
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exclusive Events"
        android:textSize="35dp"
        android:onClick="onCheckboxClicked"
        android:id="@+id/exclusive_event"
        android:layout_below="@+id/all_events_id"
        />
    <CheckBox
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Normal"
        android:textSize="35dp"
        android:id="@+id/normal_id"
        android:onClick="onCheckboxClicked"
        android:layout_below="@+id/exclusive_event"
        />
    <CheckBox
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="All"
        android:textSize="35dp"
        android:id="@+id/all_events_id"
        android:onClick="onCheckboxClicked"

        />
    <CheckBox
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="People"
        android:textSize="35dp"
        android:id="@+id/people_id"
        android:onClick="onCheckboxClicked"
        android:layout_below="@+id/normal_id"/>


</RelativeLayout>
